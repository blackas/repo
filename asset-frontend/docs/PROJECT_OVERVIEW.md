# Asset Frontend 프로젝트 문서

**프로젝트명**: Asset Folio Frontend
**마지막 업데이트**: 2025-12-10
**버전**: 0.1.0

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택](#기술-스택)
3. [프로젝트 구조](#프로젝트-구조)
4. [핵심 기능](#핵심-기능)
5. [페이지 및 라우팅](#페이지-및-라우팅-pages-and-routing)
6. [시작하기](#시작하기)
7. [아키텍처 설계](#아키텍처-설계)
8. [API 연동](#api-연동)
9. [상태 관리](#상태-관리)
10. [인증 시스템](#인증-시스템)
11. [개발 가이드](#개발-가이드)

---

## 프로젝트 개요

Asset Folio는 한국 주식, 미국 주식, 암호화폐 등 다양한 자산을 한 곳에서 추적하고 관리할 수 있는 종합 자산 관리 프론트엔드 애플리케이션입니다.

### 핵심 목표
- **멀티 마켓 지원**: 한국/미국 주식, 암호화폐를 하나의 인터페이스에서 관리
- **사용자 맞춤 기능**: 사용자 프로필, 관심종목 등 개인화 기능 제공
- **범용 컴포넌트**: 자산 타입에 관계없이 재사용 가능한 컴포넌트 설계
- **반응형 디자인**: 모바일 친화적 UI
- **실시간 데이터**: 백엔드 API와 연동하여 실시간 시장 데이터 제공
- **확장 가능한 구조**: 새로운 자산 타입 추가가 용이한 아키텍처

### 주요 특징
✅ OAuth 2.0 기반 인증 (JWT Token)
✅ 사용자 프로필 및 관심종목 관리
✅ Material-UI를 활용한 모던 UI/UX
✅ Zustand 경량 상태 관리
✅ TypeScript로 타입 안정성 확보
✅ Vite로 빠른 개발 경험
✅ Recharts를 통한 데이터 시각화

---

## 기술 스택

### Core Framework
- **React 19.2.0**: 최신 React 기능 활용
- **TypeScript 5.9.3**: 정적 타입 체크
- **Vite 7.2.4**: 빠른 번들링 및 HMR

### UI/UX
- **Material-UI (MUI) 7.3.5**: 컴포넌트 라이브러리
- **Recharts 3.5.1**: 차트 및 그래프 시각화
- **React Hook Form**: 폼 상태 관리

### State Management & Routing
- **Zustand 5.0.9**: 경량 상태 관리
- **React Router DOM 7.9.6**: 클라이언트 사이드 라우팅

### API & Data Fetching
- **Axios 1.13.2**: HTTP 클라이언트

### Development Tools
- **ESLint 9.39.1**: 코드 린팅
- **TypeScript ESLint 8.46.4**: TS 전용 린트 규칙
- **Vite**: 빠른 개발 서버 및 빌드 도구

---

## 프로젝트 구조

```
asset-frontend/
├── src/
│   ├── components/
│   │   ├── auth/
│   │   ├── common/
│   │   └── layout/
│   ├── pages/
│   │   ├── Dashboard.tsx
│   │   ├── LoginPage.tsx
│   │   ├── RegisterPage.tsx
│   │   ├── AssetListPage.tsx
│   │   ├── AssetDetailPage.tsx
│   │   ├── UserProfilePage.tsx  # 사용자 프로필 페이지
│   │   └── WatchlistPage.tsx    # 관심종목 관리 페이지
│   ├── services/
│   │   ├── api.ts
│   │   ├── authService.ts
│   │   ├── userService.ts     # 사용자 관련 API
│   │   └── watchlistService.ts # 관심종목 API
│   ├── store/
│   │   ├── useAuthStore.ts
│   │   ├── assetStore.ts      # (예시, 필요시 생성)
│   │   └── useWatchlistStore.ts # 관심종목 상태
│   ├── types/
│   │   ├── index.ts
│   │   └── auth.ts
│   ├── utils/
│   │   ├── tokenStorage.ts
│   │   └── toast.ts
│   ├── App.tsx
│   └── main.tsx
...
```

---

## 핵심 기능

### 1. 멀티 마켓 자산 관리
- **한국 주식** (kr-stock): KOSPI, KOSDAQ 종목 추적
- **미국 주식** (us-stock): NYSE, NASDAQ 종목 추적
- **암호화폐** (crypto): Upbit 코인 추적

### 2. 사용자 프로필 관리
- **프로필 조회 및 수정**: 사용자는 자신의 이메일, 전화번호 등 개인 정보를 수정할 수 있습니다.
- **일일 리포트 수신 설정**: 데일리 리포트 수신 여부를 설정할 수 있습니다.

### 3. 관심종목 (Watchlists)
- **생성, 수정, 삭제**: 사용자는 여러 개의 관심종목 그룹을 자유롭게 관리할 수 있습니다.
- **자산 추가/제거**: 자산 상세 페이지에서 원하는 관심종목에 자산을 쉽게 추가하거나 제거할 수 있습니다.

### 4. 자산 목록 및 상세 정보
- **자산 목록 페이지**: 검색, 필터링, 페이지네이션 기능 제공
- **자산 상세 페이지**: 가격 차트, 기본 정보, 관련 뉴스 등 제공

### 5. 인증 시스템
- **OAuth 2.0 기반 로그인**: Password Grant 및 Refresh Token Grant 지원
- **자동 토큰 갱신**: Axios Interceptor를 통해 만료된 Access Token을 자동으로 갱신합니다.

---

## 페이지 및 라우팅 (Pages and Routing)

애플리케이션의 주요 페이지와 URL 경로는 다음과 같이 구성됩니다.

- **로그인 페이지**: `/login`
  - 사용자가 이메일과 비밀번호로 로그인합니다.

- **회원가입 페이지**: `/register`
  - 새로운 사용자가 계정을 생성합니다.

- **대시보드**: `/dashboard`
  - 로그인 후 가장 먼저 보게 되는 페이지로, 자산 요약 및 주요 지수를 표시합니다.

- **자산 목록 페이지**: `/assets/{market}`
  - `market` 파라미터에 따라 `kr-stock`, `us-stock`, `crypto` 등 특정 시장의 자산 목록을 표시합니다.
  - **예시**: `/assets/kr-stock` (한국 주식 목록)

- **자산 상세 페이지**: `/assets/{market}/{assetId}`
  - 특정 자산의 상세 정보(차트, 뉴스 등)를 표시합니다.
  - **예시**: `/assets/kr-stock/005930` (삼성전자 상세 정보)

- **관심종목 페이지**: `/watchlist`
  - 사용자가 생성한 관심종목 목록과 각 그룹에 속한 자산을 보여줍니다.

- **사용자 프로필 페이지**: `/profile`
  - 사용자의 개인 정보를 조회하고 수정합니다.

---

## 시작하기

(내용 동일)

---

## API 연동

### API 엔드포인트 목록

#### 인증 API
- `POST /auth/token`: 로그인 / 토큰 갱신
- `POST /auth/register`: 회원가입
- `GET /auth/userinfo`: 현재 사용자 정보
- `POST /auth/logout`: 로그아웃

#### 사용자 API
- `GET /users/me`: 내 프로필 정보 조회
- `PATCH /users/me`: 내 프로필 정보 수정

#### 관심종목 API
- `GET /watchlists`: 내 관심종목 목록 조회
- `POST /watchlists`: 새 관심종목 생성
- `PATCH /watchlists/{id}`: 관심종목 이름 수정
- `DELETE /watchlists/{id}`: 관심종목 삭제
- `GET /watchlists/{id}/assets`: 관심종목 내 자산 목록 조회
- `POST /watchlists/{id}/assets`: 관심종목에 자산 추가
- `DELETE /watchlists/{id}/assets/{asset_id}`: 관심종목에서 자산 제거

#### 자산 API
- `GET /stocks`: 한국 주식 목록
- `GET /stocks/{id}`: 주식 상세
- `GET /stocks/{id}/prices`: 주가 데이터
- `GET /crypto`: 암호화폐 목록

---

## 상태 관리

### Zustand 스토어 구조

#### 1. 인증 스토어 (`store/useAuthStore.ts`)
- 사용자 정보, 인증 상태, 로그인/로그아웃 함수 등을 관리합니다.

#### 2. 관심종목 스토어 (`store/useWatchlistStore.ts`)
- 관심종목 목록, 선택된 관심종목, 관련 CRUD 함수 등을 관리합니다.

```typescript
// store/useWatchlistStore.ts
import { create } from 'zustand';
// ...

interface WatchlistState {
  watchlists: Watchlist[];
  selectedWatchlist: Watchlist | null;
  // ...
  fetchWatchlists: () => Promise<void>;
  createWatchlist: (name: string) => Promise<void>;
  // ...
}

export const useWatchlistStore = create<WatchlistState>((set) => ({
  // ...
}));
```

---

## 개발 가이드

(내용 동일)

---

**최종 업데이트**: 2025-12-10
**문서 버전**: 1.1.0